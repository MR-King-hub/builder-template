{
  "name": "devtools",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run --prefix ../template build",
    "preview": "npm run --prefix ../template preview",
    "all": "npm run --prefix ../template build && pm2 delete all 2>/dev/null; pm2 start ecosystem.config.cjs && sleep 8 && pm2 logs cloudflare-tunnel --lines 50 --nostream 2>/dev/null | grep -oE 'https://[a-z0-9-]+\\.trycloudflare\\.com' | tail -1 | xargs -I {} echo '<!-- TUNNEL_URL: {} -->'",
    "dev": "pm2 delete all 2>/dev/null; pm2 start ecosystem.config.cjs && sleep 8 && pm2 logs cloudflare-tunnel --lines 50 --nostream 2>/dev/null | grep -oE 'https://[a-z0-9-]+\\.trycloudflare\\.com' | tail -1 | xargs -I {} echo '<!-- TUNNEL_URL: {} -->'",
    "stop": "pm2 stop all",
    "restart": "pm2 restart all",
    "status": "pm2 status",
    "logs": "pm2 logs",
    "url": "pm2 logs cloudflare-tunnel --lines 50 --nostream 2>/dev/null | grep -oE 'https://[a-z0-9-]+\\.trycloudflare\\.com' | tail -1 | xargs -I {} echo '<!-- TUNNEL_URL: {} -->'"
  },
  "devDependencies": {
    "cloudflared": "^0.7.1",
    "concurrently": "^9.2.1",
    "vite": "^6.0.0"
  }
}
